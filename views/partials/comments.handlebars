<div class="comments">
    {{#each this}}

    <section class="comment">
        <div class="meta">
            {{user.name}}
            time
            {{format_date createdAt}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
    {{#if loggedIn}}

</div>